<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <title>All Product</title>
</head>
<body>
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Home page</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mynavbar">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mynavbar">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link" href="/view">All Products</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="">Hot Sale</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="">News</a>
        </li>
        
      </ul>
      <form action="/search" method="post" class="d-flex">
        <input class="form-control me-2" type="text" placeholder="Search" name="txtSearch">
        <button class="btn btn-primary" type="Search">Search</button>
      </form>
    </div>
  </div>
</nav>
    <h1>All product</h1>
    {{!-- <form action="/search" method="post">
    <input type="text" name="txtSearch" >
    <input type="submit" value="Search">
    </form> --}}

<div class="container mt-3">
  <table class="table table-dark">
    <a href="/new"><button type="button" class="btn btn-light">Add new product</button></a>
    <thead>
      <tr>
        
        <th>Name</th>
        <th>Price
          {{!-- <a href="/asc">A</a> --}}
        </th>
        <th>Color</th>
        <th>Image</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each prods}}
      
      <tr>
        {{!-- {{#if (check1 color)}}
        <td style="color:purple">{{name}}</td>
        <td style="color:purple">{{price}}</td>
        <td style="color:purple">{{color}}</td>
        {{!-- <td>{{color}}</td> --}}
        {{!-- <td style="color:purple"><a href="">
          <img src="{{image}}" width="100" alt="Image">
        </a></td>
        
        <td style="color:purple"><a href="/delete?id={{_id}}"><button type="button" class="btn btn-light">Delete</button></a></td>
        {{else}}  --}} 
        <td>{{name}}</td>
        {{#if (check price)}}
        <td style="color:red">{{price}}$</td>
        {{else}} 
        <td>{{price}}$</td>
        {{/if}}
        <td>{{color}}</td>
        <td><a href="">
          <img src="{{image}}" width="100" alt="Image">
        </a></td>
        <td><a href="/delete?id={{_id}}"><button type="button" class="btn btn-light">Delete</button></a></td>
        {{!-- {{/if}} --}}
      </tr>
      
      {{/each}}
    </tbody>
  </table>
</div>
    {{!-- <ul>
    {{#each prods}}
    <li>{{name}} - {{price}} <a href="/delete?id={{_id}}">Delete</a></li>
    {{/each}}
    
    </ul> --}}
</body>
</html>